[LdapServersTestCase]
name = LDAP Servers Tests
description = Test ldap servers. Servers module is primarily a storage tool for ldap server configuration, so most of testing is just form and db testing. There are some api like functions that are also tested.
group = LDAP Servers
file = ldap_servers.test
